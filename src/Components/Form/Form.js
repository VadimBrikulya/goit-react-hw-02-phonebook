import React, { Component } from 'react';
class Form extends Component {
    render() {
        return (
            <div>
                <input type="tel" placeholder = "Name" />
            </div>
        );
    }
}

export default Form;